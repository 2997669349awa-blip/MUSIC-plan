<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- 查询所有包权限：Android 11+ 包可见性限制需要此权限才能查询/启动其他应用
         （如三角洲行动）。私有应用使用，不上 Play Store。 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- 显式声明要查询的包名（双保险，即使 QUERY_ALL_PACKAGES 被限制也能查询） -->
    <queries>
        <package android:name="com.tencent.tmgp.dfm" />
        <package android:name="moe.shizuku.privileged.api" />
        <package android:name="rikka.shizuku" />
        <package android:name="com.example.musicplugin" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.FileManager"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TextViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.FileManager" />

        <activity
            android:name=".ApkToolsActivity"
            android:exported="false"
            android:theme="@style/Theme.FileManager" />

        <activity
            android:name=".ImageViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.FileManager" />

        <activity
            android:name=".MusicPlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.FileManager"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.FileManager" />

        <activity
            android:name=".TerminalActivity"
            android:exported="false"
            android:theme="@style/Theme.FileManager"
            android:windowSoftInputMode="adjustResize" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Shizuku Provider - 必须配置才能调用Shizuku API -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>

</manifest>
